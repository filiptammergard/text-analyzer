minimumReleaseAgeExclude:
  - "@tammergard/*"

# Security floors for dev-only transitive dependencies. Remove each entry once
# the upstream dependency (vitest) resolves to a patched version on its own.
overrides:
  # GHSA-v6wh-96g9-6wx3, GHSA-mv2q-cxrp-99qh (via vitest)
  vite: "^8.2.1"
  # GHSA-6g55-p6wh-862q, GHSA-fxqj-rqcc-2cmp (via vitest > vite)
  postcss: "^8.5.26"
  # GHSA-qx8q-tvhc-97gm, GHSA-3pv3-2h8v-8h4m (via vitest > vite > postcss)
  nanoid: "^3.3.18"
